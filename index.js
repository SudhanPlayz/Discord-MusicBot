const DiscordMusicBot = require("./lib/DiscordMusicBot"); 
const client = new DiscordMusicBot();

client.build() // Make sure your botconfig.js is filled out :p

module.exports = client;//We will just have interaction commands here
//Bruh ok good luck, Don't remove my dashboard tho lol
